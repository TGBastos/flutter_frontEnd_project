import 'package:flutter/material.dart';

class LoginPage extends StatelessWidget {
  final Widget? child;
  const LoginPage({Key? key, required this.child}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class LogedPage extends StatelessWidget {
  final Widget? child;
  const LogedPage({Key? key, required this.child}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class MensagesPage extends StatelessWidget {
  final Widget? child;
  const MensagesPage({Key? key, required this.child}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class SingupPage extends StatelessWidget {
  final Widget? child;
  const SingupPage({Key? key, required this.child}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
